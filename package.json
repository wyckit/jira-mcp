{
  "name": "jira-mcp",
  "version": "2.1.0",
  "description": "MCP server for Jira Server/Data Center — research and analysis tools over Jira, authenticated with a Personal Access Token. Runs with zero dependencies.",
  "type": "module",
  "main": "server.js",
  "bin": {
    "jira-mcp": "./server.js"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node test/run-tests.mjs"
  },
  "keywords": [
    "mcp",
    "model-context-protocol",
    "jira",
    "jira-data-center",
    "claude",
    "zero-dependency"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "comment": "Dependencies are optional. The server ships with a bundled dependency-free MCP/stdio implementation in lib/ and uses the official SDK only when it is present. See README 'Install' for the no-npm path.",
  "optionalDependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  }
}
