node_modules/
.env
.env.*
*.log

# Local credentials — never commit, never package
jira-mcp.config.json
.jira-mcp.json
.DS_Store
Thumbs.db

# Build artifacts — the .exe ships as a GitHub Release asset, not in the repo
dist/
build/bundle.cjs
build/sea-config.json
build/sea-prep.blob
